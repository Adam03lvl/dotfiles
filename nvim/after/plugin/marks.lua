require('marks').setup {
    default_mappings = false,
    mappings = {
        set = 'm',
        delete = 'dm',
        set_next = 'm,',
        next = 'mM',
        prev = 'm<c-m>',
        delete_buf = false
    }
}
